#include<bits/stdc++.h>

using namespace std;

main(){
	int t; cin >> t;
	cin.ignore();
	while(t--){
		string s; cin >> s;
	    int cnt[100000]={0};
		for(int i=0;i<s.length();i++) cnt[s[i]]++;
		for(int i=0;i<s.length();i++){
			if(cnt[s[i]]==1) cout << s[i];
		}
		cout << endl;
	}
}
