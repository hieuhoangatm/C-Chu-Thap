#include<bits/stdc++.h>

using namespace std;

main(){
	int a[10000],n;
	cin >> n;
	for(int i=0;i<n;i++) cin >> a[i];
	set<int> s;
	for(int i=0;i<n;i++) s.insert(a[i]);
	for(int x:s) cout << x << " ";
	
}
