#include<bits/stdc++.h>

using namespace std;

main(){
	int t; cin >> t;
	while(t--){
		int n,x; cin >> n>>x;
		int a[n],dem=0;
		for(int i=0;i<n;i++){
			cin >> a[i];
			if(a[i]==x) dem++;
		}
		if(dem!=0) cout <<dem << endl;
		else cout << "-1\n";
		
	}
}
