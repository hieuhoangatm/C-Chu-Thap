#include<bits/stdc++.h>

using namespace std;

main(){
	int t; cin >> t;
	cin.ignore();
	while(t--){
		string s; cin >> s;
		int k; cin >> k;
		cin.ignore();
		set<char> se;
		for(char x:s) se.insert(x);
		if(k>=(26-se.size())) cout << "1\n";
		else cout << "0\n";
    }
}
